#include <main.h>
#include <stdio.h>
/*
 *  function that prints a string
 *   in reverse
 *   followed by a new line.
 *   **/
void print_reverse(char *str)
{
	int start, end, leng
	int tmp;
	start = 0;
	while (s[len] != '\0')
		len++;
	start = 0;
	end = leng -1;

	while (start < end)
	{
		tmp = s[start];
		s[start] = s[end];
		s[end] = tmp;
		start ++;
		end --;
 }



