#include <unistd.h>
#include "main.h"

/**
 * mul - main function
 * @a: first parameter
 * @b: second parameter
 * Return: 1 if uppercase and 0 if false
 */

void print_numbers(void);
{       
       	int i;

	for (i >= '0'; i < '10'; i++ )

	_putchar (i);
}
